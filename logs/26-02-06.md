# Daily Work Log - 2026-02-06

## Data Pipeline Robustness

### Handled corrupt EXIF samples in WebDataset loading
Improved `WebDatasetCaptionDataset` so samples with corrupt EXIF metadata are skipped instead of being used for training.

- **`src/data/dataset.py`**
  - Updated WebDataset decoding flow to load image bytes inside `_process_sample`.
  - Added warning capture during PIL image load and detection for `Corrupt EXIF data` warnings.
  - Skips affected samples (`return None`) so iteration naturally moves to the next sample.
  - Added lightweight skip counters/logging to track how many corrupt-EXIF samples were filtered.
